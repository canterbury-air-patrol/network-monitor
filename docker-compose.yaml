version: '3'

services:
  db:
    image: mdillon/postgis
  app:
    build: .
    volumes:
      - ./:/code/
    ports:
      - 8050:8050
    depends_on:
      - db
  test:
    build: .
    volumes:
      - ./:/code/
    depends_on:
      - db
    command: pytest
